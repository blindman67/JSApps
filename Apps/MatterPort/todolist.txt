TODOs 

Performance test to determin best style. Code will be marked with comment and fixed when testing is complete

/* A100 */   destructure array assignment V ternary. 
    Example
            [a, b] = foo ? [aa, bb] : [bb, aa];
        V
            for ? (a = aa, b = bb) : (a = bb, b = aa);

/* A101 */   destructure array declartion assignment assignment V multy-line assignment
    Example
            const [a, b] = [aa, bb];
        V
            const a = aa, b = bb;

/* A */   
    Example
        V
       